<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/card_view"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardCornerRadius="16dp"
    android:elevation="8dp"
    android:backgroundTint="@color/black">

    <LinearLayout
        android:id="@+id/card_inner_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/card_border_template"
        android:gravity="center_horizontal"
        android:orientation="vertical"
        android:padding="16dp">

        <TextView
            android:id="@+id/card_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/card_title"
            android:textColor="@color/white"
            android:textSize="22sp"
            android:textStyle="bold"
            android:layout_marginBottom="15dp"/>

        <!-- FrameLayout to center the Add Icon and RadialBalanceView -->
        <FrameLayout
            android:layout_width="120dp"
            android:layout_height="120dp">

            <za.ac.iie.opsc_poe_screens.ui.RadialBalanceView
                android:id="@+id/radial_balance_view"
                android:layout_width="match_parent"
                android:layout_height="match_parent" />

            <!-- Icon for the "Add" button, centered and hidden by default -->
            <ImageView
                android:id="@+id/add_icon"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:src="@android:drawable/ic_input_add"
                android:visibility="gone"
                android:layout_gravity="center"
                app:tint="@color/white" />

        </FrameLayout>

        <TextView
            android:id="@+id/card_balance"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/r_0_00"
            android:textColor="@color/white"
            android:layout_marginTop="15dp"
            android:textSize="21sp" />

    </LinearLayout>

</androidx.cardview.widget.CardView>