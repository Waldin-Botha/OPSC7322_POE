<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="4dp"
    app:cardBackgroundColor="@color/light_green">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="12dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <TextView
                android:id="@+id/tvGoalName"
                android:layout_width="0dp"
                android:layout_weight="1"
                android:layout_height="wrap_content"
                tools:text="Monthly Food Budget"
                android:textSize="18sp"
                android:textColor="@color/black"
                android:textStyle="bold"/>

            <!-- START: New Status Icons -->
            <!-- Bonus Goal Indicator (Purple Star) -->
            <ImageView
                android:id="@+id/ivBonusIndicator"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:layout_marginStart="8dp"
                android:src="@drawable/ic_star"
                app:tint="@color/purple"
                android:visibility="gone"
                android:contentDescription="Bonus Goal Indicator"
                tools:visibility="visible"/>

            <!-- Completion Status Indicator (Green/Gray Check) -->
            <ImageView
                android:id="@+id/ivCompletedIndicator"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:layout_marginStart="8dp"
                android:src="@drawable/ic_check_circle"
                android:visibility="gone"
                android:contentDescription="Goal Completion Status"
                tools:visibility="visible"
                tools:tint="@android:color/holo_green_dark"/>
            <!-- END: New Status Icons -->


            <!-- Action Buttons -->
            <ImageButton
                android:id="@+id/btnEditGoal"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:layout_marginStart="8dp"
                android:background="@null"
                android:src="@android:drawable/ic_dialog_info"
                android:contentDescription="@string/edit_goal1"/>

            <ImageButton
                android:id="@+id/btnDeleteGoal"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:src="@android:drawable/ic_menu_delete"
                app:tint="@color/red"
                android:contentDescription="@string/delete_goal"/>

        </LinearLayout>

        <!-- Progress Bar -->
        <ProgressBar
            android:id="@+id/pbGoalProgress"
            style="@android:style/Widget.ProgressBar.Horizontal"
            android:layout_width="match_parent"
            android:layout_height="12dp"
            android:layout_marginTop="8dp"
            android:progressTint="@color/accent_green"
            android:progressBackgroundTint="@color/light_grey"
            android:max="100"/>

        <TextView
            android:id="@+id/tvProgressText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            tools:text="R 1500 / R 2000"
            android:textColor="@color/black"
            android:textSize="14sp"
            android:layout_marginTop="4dp"/>

    </LinearLayout>
</androidx.cardview.widget.CardView>